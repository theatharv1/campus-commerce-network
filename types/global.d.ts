// Ambient type declarations shared across the CCN workspace.
// Package-specific and app-specific types are introduced with their
// packages in Stage 14 (Monorepo Architecture). This file exists so the
// type-checker has a valid input on a fresh clone before packages exist.

export {};
