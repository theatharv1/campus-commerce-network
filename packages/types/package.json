{
  "name": "@ccn/types",
  "version": "0.0.0",
  "private": true,
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "lint": "eslint .",
    "typecheck": "tsc --noEmit",
    "test": "jest",
    "test:coverage": "jest --coverage",
    "clean": "rm -rf dist .turbo coverage"
  },
  "devDependencies": {
    "@ccn/config": "workspace:*",
    "jest": "catalog:",
    "ts-jest": "catalog:",
    "@types/jest": "catalog:"
  }
}
